import {
  breakpoints,
  breakpointMappings,
  elementSizes,
  fontSizes,
  space
} from './sizes'
import {fonts} from './fonts'
import {colors} from './colors'
import {shadows} from './box-shadow'

const theme = {
  breakpoints,
  breakpointMappings,
  elementSizes,
  fontSizes,
  fonts,
  space,
  colors,
  shadows
}

export default theme
